#include<stdio.h>
int main()
{
    int a[10000],i,n;
    scanf("%d",&n);
    if(n<=1000)
    {
        for(i=0;i<=n;i++)
        {
            scanf("%d",&a[i]);
        }
        for(i=n-1;i>=0;i--)
        {
            printf("%d ",a[i]);
        }
    }
    return 0;
}
